# name: Build and Deploy

# on:
#   push:
#     branches:
#       - main

# jobs:
#   build:
#     name: Build and Deploy Go App
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v2

#       - name: Set up Go
#         uses: actions/setup-go@v2
#         with:
#           go-version: '1.2'

#       - name: Build
#         run: go build -o myapp

#       - name: Create Release
#         id: create_release
#         uses: actions/create-release@v1
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#         with:
#           tag_name: v1.0.0 # Replace with your desired version tag
#           release_name: Release v1.0.0 # Replace with your desired release name
#           body: Release notes for v1.0.0 # Replace with your release notes
#           draft: false
#           prerelease: false

#       - name: Upload Release Artifact
#         id: upload-release-asset
#         uses: actions/upload-release-asset@v1
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#         with:
#           upload_url: ${{ steps.create_release.outputs.upload_url }}
#           asset_path: ./myapp
#           asset_name: myapp
#           asset_content_type: application/octet-stream
